# Copilot Instructions

## Project Guidelines
- For FuzzyRewardMatcher, OCR input is expected to resolve to a single reward item rather than multiple items, and tests should include noisy OCR strings (e.g. '2 s w a as h prime neuroptics blueprint').